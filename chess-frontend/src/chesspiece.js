const chess_piece_position = new Map();

isValidPosition(i,j){

}
export default function ChessPiece(i,j){
      return (
        <React.Fragment>
            <div id="ChessPiece">
                isValidPosition(i,j);
            </div>
        </React.Fragment>
      )
}